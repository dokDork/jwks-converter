# jwks-converter
This script converts RSA public key parameters (n and e) from a JWKS JSON file into a PEM-formatted public key. It takes the JWKS file path as input and outputs the corresponding PEM key.
